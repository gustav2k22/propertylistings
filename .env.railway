# Railway environment variables
NODE_ENV=production

# Database connection string - this will be set by Railway
# MYSQL_URL=mysql://username:password@hostname:port/database

# Fallback database configuration (in case MYSQL_URL is not set)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=property_listings
DB_PORT=3306

# Server configuration
PORT=3000
